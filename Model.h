#include <iostream>
using namespace std;

class Model{
    public:
        Model();
        ~Model();
        string translateSingleCharacter(char singleChar);
        string translateDoubleCharacter(char doubleChar);
};
